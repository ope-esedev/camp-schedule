# camp-schedule

Static camp schedule generator.

Vercel hosting:

- Deploy the repository as a static project.
- The site now includes a root `index.html`, so Vercel can serve it as a standard static deployment with no build command.
- The root page redirects to `schedule.html`.
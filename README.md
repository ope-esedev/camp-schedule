# camp-schedule

Static camp schedule generator.

Vercel hosting:

- Deploy the repository as a static project.
- The Vercel config rewrites `/` to `schedule.html`, so no framework or build command is required.